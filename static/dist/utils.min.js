window.Utils ={dom:{get:(selector)=> document.querySelector(selector), getAll:(selector)=> Array.from(document.querySelectorAll(selector)), on:(element, event, handler)=> element && element.addEventListener(event, handler), create:(tag, attrs ={}, content = '')=>{const el = document.createElement(tag); Object.entries(attrs).forEach(([k, v])=>{if(k === 'className')el.className = v; else if(k === 'textContent')el.textContent = v; else el.setAttribute(k, v);}); if(content)el.innerHTML = content; return el;}}, storage:{get:(key)=> localStorage.getItem(key), set:(key, value)=> localStorage.setItem(key, value), remove:(key)=> localStorage.removeItem(key)}, debounce:(func, wait)=>{let timeout; return function executedFunction(...args){const later =()=>{clearTimeout(timeout); func(...args);}; clearTimeout(timeout); timeout = setTimeout(later, wait);};}}; console.log('✅ Nuclear Utilities loaded');